import 'package:flutter/cupertino.dart';

class GameFormExamplePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello from entery page"),
    );
  }
}
